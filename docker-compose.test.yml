services:
  qdrant:
    image: qdrant/qdrant:v1.12.1
    ports:
      - "6333:6333"
      - "6334:6334"
    # Ephemeral by design — the test creates and drops its own throwaway
    # collection, so no volume is mounted.
